import { ContainerLoader, Circleload } from "./style";

const Loader = () => {
    return (
        <ContainerLoader>
            <Circleload />
        </ContainerLoader>
    );
};

export default Loader;
